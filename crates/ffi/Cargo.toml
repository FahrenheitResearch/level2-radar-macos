[package]
name = "ffi"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["staticlib"]

[dependencies]
met-engine = { path = "../met-engine" }
radar-domain = { path = "../radar-domain" }
